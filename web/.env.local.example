# Copy to .env.local and fill in. These are the PUBLIC (anon) Supabase values —
# safe to expose in the browser. The service_role key NEVER goes here; it lives
# only in the engine's .env and in GitHub Actions secrets.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
